<a target="{{ link.target|default('_blank') }}" class="{{ link.class }}" href="{{ link.href }}">{{ link.text }}</a>

<a target="{{ link.target|default('_blank') }}" class="{{ link.class }}" href="{{ link.href }}">{{ link.text }}<span class="icon-arrow-right"></span></a>
